# Karabiner rewrites these on every change
karabiner/.config/karabiner/automatic_backups/

# machine-specific / secrets live outside the repo
zsh/.zshrc.local

# editor / OS noise
.DS_Store
nvim/.config/nvim/lazy-lock.json.bak
.scratch/

# wallpapers are downloaded by omacos-theme into ~/.local/share/omacos/backgrounds (outside the repo)

# Sol runtime state
sol/.config/sol/state.json
sol/.config/sol/images_pasteboard/

shell/.build/

# personal, never committed
omacos/.config/omacos/weather-location
